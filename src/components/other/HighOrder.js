/*
A higher-order component is just a function that takes an existing
component and returns another component that wraps it.
*/

function highOrder (Component) {
  const HighOrder = React.createClass({

  })

  return HighOrder
}
